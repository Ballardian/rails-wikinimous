<div class = "container-test">
    <% @articles.each do |article| %>
      <div class = "card">
      <%= link_to article.title, article_path(article.id) %>
      </div>
    <% end %>
    <div class="btn btn-primary"><%= link_to "Create new article", new_article_path %></div>
</div>
